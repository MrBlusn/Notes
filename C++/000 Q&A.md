1. 范围 for 循环和传统 for 循环有区别吗，前者会达到条件停止吗

|特性|传统 for 循环|范围 for 循环|
|:--|:--|:--|
|控制方式|手动控制索引、条件、步长|自动遍历整个范围|
|停止条件|显式写出（如 `i < n`）|没有条件，遍历完就停|
|是否可能越界|可能（写错条件）|不会（由编译器控制）|
|是否支持中途跳出|支持（`break`、`return`）|支持（`break`、`return`）|
|是否支持修改容器结构|可以（但危险）|不可以（可能失效）|

